import React from "react";
import { name, city } from "../data/data.js";

function Home(props) {
  <div>
    <h2>{name}</h2>
    <h2>{city}</h2>
  </div>
  return <div>Home</div>;
}

export default Home;
